const HomeView = () => {

    return  (
        <div>
            <h2 className="text-center">Datasets</h2>

            <Datasets />
        </div>
    )
}

import Datasets from "../datasets/Datasets.tsx";

export default HomeView